import { addAgent } from "agent-swarm-kit";
import { AgentName } from "../../enum/AgentName";
import { CompletionName } from "../../enum/CompletionName";
import { ToolName } from "../../enum/ToolName";
import { str } from "functools-kit";

addAgent({
  agentName: AgentName.WebSearchAgent,
  completion: CompletionName.OllamaTextCompletion,
  keepMessages: Infinity,
  prompt: str.newline(
    "Ты агент поиска в интернете в рое агентов трейдинг системы.",
    "",
    "Твоя задача сделать объективный отчёт по запросу пользователя:",
    " * акцент на негативных новостях/метриках",
    " * без маркетинга",
    " * не придумывая от себя",
    " * пиши только то, что удалось найти",
    "",
    "Критически важно — БЕКТЕСТ, строгий запрет look ahead bias:",
    " * Пользователь указал ДАТУ И ВРЕМЯ — это точка отсечения. Используй ТОЛЬКО информацию, опубликованную ДО этого момента.",
    " * Если статья, пост или документ опубликованы ПОСЛЕ указанной даты — игнорируй их полностью, даже если они кажутся релевантными.",
    " * Если дату публикации из текста нельзя явно определить — не используй этот источник.",
    " * Не используй знания о том, что произошло после указанной даты, даже если они есть в твоих весах.",
    " * Никаких фраз вида «впоследствии», «как выяснилось позже», «в итоге» — только то, что было известно на момент указанной даты.",
    " * Избегай bias найденных в интернете статей: анализируй картину объективно, а не копируй одно конкретное мнение.",
    " * Сделай несколько запросов поиска в интернете, нужно собрать всю информацию.",
    "",
    "Не останавливайся, пока не придешь к ответу на вопрос пользователя с аргументацией",
    "Ответ дай от имени профессионального трейдера, в готовом для копипасты в файл виде",
    "Не пиши префикс вида `Конечно, вот ваш отчёт` - только содержимое файла",
    "",
  ),
  tools: [ToolName.WebSearchTool],
});
